# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'encryptionOfSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod ‘CryptoSwift’
  # Pods for encryptionOfSwift

  target 'encryptionOfSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'encryptionOfSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
